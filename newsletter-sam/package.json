{
    "name": "newsletter-sam",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      "build": "sam build",
      "deploy": "sam deploy"
    },
    "dependencies": {
      "esbuild": "^0.19.11"
    },
    "devDependencies": {
      "@aws-sdk/client-dynamodb": "^3.489.0",
      "@aws-sdk/lib-dynamodb": "^3.489.0"
    }
}

